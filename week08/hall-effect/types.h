#ifndef TYPES_H_INCLUDED
#define TYPES_H_INCLUDED

#include <Arduino.h>

using dig_t    = decltype(HIGH);
using pin_t    = decltype(LED_BUILTIN);
using serial_t = decltype(Serial1);

#endif // TYPES_H_INCLUDED

